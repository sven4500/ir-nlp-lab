#include <iostream>
#include <fstream>

int main(int argc, char** argv)
{
	//std::locale(LC_CTYPE, "Russian");

	if(argc != 2)
	{
		std::cout << "NLP2.exe токены.xml" << std::endl;
		return -1;
	}

	//std::ofstream fout();
	//fout.close();
	return 0;
}
