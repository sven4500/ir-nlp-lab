#include "studproc.h"

StudProc::StudProc()
{}

StudProc::~StudProc()
{}

void StudProc::update(Collector const& monograms, BigramCollector const& bigrams)
{
    for(unsigned int i = 0; i < bigrams.count(); ++i)
    {
    }
}
